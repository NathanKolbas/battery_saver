import 'package:flutter/material.dart';

class AuthenticatorMfa extends StatelessWidget {
  const AuthenticatorMfa({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
