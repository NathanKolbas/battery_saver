import 'package:flutter/material.dart';

class PhoneMfa extends StatelessWidget {
  const PhoneMfa({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
